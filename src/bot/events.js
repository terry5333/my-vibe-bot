"use strict";

/**
 * src/bot/events.js
 * ✅ A 方案：這檔案改成 no-op，避免重複綁 interactionCreate / messageCreate
 * 如果你之前 index.js 有 require 這個檔案，保留它也不會再造成「跑兩次」
 */

module.exports = function bindEvents() {
  // no-op
};